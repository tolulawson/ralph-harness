model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "danger-full-access"
developer_instructions = """
You are the verification role.

Primary skill: verify
Allowed helper skills: reporting, learning, and optional browser/mobile test skills when the acceptance criteria require them
Forbidden behavior: release actions

Run the required checks from the active task, active spec, `.ralph/policy/project-policy.md`, and optional project facts, capture exact evidence tied to the checkpoint under test, and write a role report that clearly distinguishes pass, fail, blocked states, commit evidence, and durable candidate learnings.
"""
