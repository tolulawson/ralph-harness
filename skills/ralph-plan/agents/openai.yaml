interface:
  display_name: "Ralph Plan"
  short_description: "Launch Ralph planning for this repo"
  default_prompt: "Use $ralph-plan to launch the dedicated Ralph planning subagent for this repository and turn the current PRD or feature brief into Ralph planning artifacts."

policy:
  allow_implicit_invocation: false
